{%- if section.settings.fake_view_enable -%}
<div class="fake-view text-center">
  <span> {{section.settings.fake_view_text_prefix}}</span>
  <span class="js-fake-view bg-theme gradient-theme-vertical p-1 text-white d-inline-block" data-min="{{section.settings.fake_view_min}}" data-max="{{section.settings.fake_view_max}}" data-duration="{{section.settings.fake_view_duration | times:1000}}"></span>
  <span> {{section.settings.fake_view_text_suffix}}</span>
</div>
{%- endif -%}